{
  "name": "ai-fitness-coach",
  "version": "1.0.0",
  "description": "AI Fitness Coach — monorepo root with client (React/Vite) and server (Express/MongoDB).",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev": "npx concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",

    "install:all": "npm --prefix client install && npm --prefix server install",

    "build": "npm --prefix client run build",
    "build:client": "npm --prefix client run build",

    "start": "npm --prefix server run start",
    "start:server": "npm --prefix server run start",

    "deploy": "npm run install:all && npm run build && npm run start"
  }
}

